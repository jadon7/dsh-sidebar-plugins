/**
 * Token poker plugin, node half.
 *
 * Deliberately empty: the feature is a browser-only interactive mockup.
 */

/** Host plugin body — the browser half carries the whole feature. */
export function apply(): void {}
