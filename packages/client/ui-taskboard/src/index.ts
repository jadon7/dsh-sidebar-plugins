/**
 * Web taskboard plugin, node half.
 *
 * Deliberately empty: the taskboard surface is pure browser UI (a sidebar
 * entry + embedded panel over the locally running Codex Taskboard service),
 * with no host-side behavior of its own.
 */

/** Host plugin body — the browser half carries the whole feature. */
export function apply(): void {}
